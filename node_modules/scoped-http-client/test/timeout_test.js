// Generated by CoffeeScript 1.10.0
(function() {
  var ScopedClient, assert, called, client, http;

  ScopedClient = require('../src');

  http = require('http');

  assert = require('assert');

  called = 0;

  client = ScopedClient.create('http://10.255.255.1:9999');

  client.timeout(100);

  client.get()(function(err, resp, body) {
    if (err) {
      return called++;
    }
  });

  process.on('exit', function() {
    return assert.equal(1, called);
  });

}).call(this);
